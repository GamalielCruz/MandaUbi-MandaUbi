// Import all schemas
import user from './user'
import event from './event'
import invitation from './invitation'
import rsvp from './rsvp'

// Export all schemas
export const schemaTypes = [user, event, invitation, rsvp]
